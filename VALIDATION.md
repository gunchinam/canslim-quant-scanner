# 판단 포스터 한줄평 연결 + 명사형 종결 재정비 — VALIDATION

> PRD `01_PRD.md` §5 성공 기준을 검증 항목으로 변환. 각 항목은 실행 결과(스크린샷/커맨드 출력)로만 통과 처리한다 — 느낌으로 통과 금지.

- [x] V1. `dp-verdict-poster`(모바일)와 `dp-hero-banner-dt`(데스크톱) 헤드라인 자리에 `d.OneLiner` 값이 표시된다. — `if (d.OneLiner) { _pvWord = esc(d.OneLiner); }` 추가(app.js:3751), 4개 종목 스크린샷으로 각기 다른 한줄평 표시 확인.
- [x] V2. `_pvReason`, `conv` 계산 로직, 배경색(`_pgCls`)/확신도(%) 표시는 변경 전과 동일하게 동작한다. — git diff상 `_pvReason`/`conv`/`_pgCls` 계산 블록 미변경, 스크린샷에서 3줄 이유·배경색·확신도%가 기존 conv 기반 문구 그대로 나옴 확인.
- [x] V3. 헤드라인에 긴 한줄평이 들어가도 카드 레이아웃이 깨지거나 텍스트가 잘리지 않는다. — scanner.css `.dvp-word`/`.dhb-word`에 `clamp()` + `word-break:keep-all` 적용, app.js에 문구 길이 기반 인라인 `font-size` 이중 적용. 4개 종목(짧은/긴 문구 혼합) 스크린샷에서 2줄 줄바꿈까지 자연스럽게 처리 확인.
- [x] V4. 18개 버킷 3533개 문구 전부가 명사형 종결로 재정비되고, 개수·버킷·극성은 리라이트 전과 정확히 동일하다. — 1차 254개(회차3~4) + 2차 119개(회차5, 회차3 정규식이 놓친 평서/의문/명령형 종결 사각지대) 총 373개 국소 교정. 반영 중 `DEFENSIVE` 버킷에서 dedup 충돌로 1개 소실되는 버그와 base 리터럴의 기존 손상 문구("...특징이라 듬") 1건을 함께 발견해 수정. 최종 `sum(len(v) for v in _PHRASES.values()) == 3533`, 18개 버킷 전부 원래 개수 유지, 중복 0건, 잔여 비명사형 종결 재스캔 결과 0건(오탐 2건은 "아이러니"/"자해"처럼 이미 명사인 경우) 확인.
- [x] V5. `tests/test_one_liner_consistency.py`가 이전 골 종료 시점(2 failed/24 passed)과 동일하게 유지된다. — 재실행 결과 2 failed/24 passed, 실패 2건은 `_bucket()` 분류 로직의 기존 결함(이번 범위 밖)으로 동일.
- [x] V6. `web_app/tests/` 전체 회귀가 기존 베이스라인과 동일하게 유지된다. — `test_history_timeline.py`(신호 히스토리 API, 이번 변경과 무관) 제외 195개 전부 통과. 해당 파일 자체는 날짜 의존적 픽스처로 실패 개수가 9~10 사이로 오락가락하는 기존 결함이며 우리 변경과 무관함을 확인.

## blocking 가정

- 없음.

## non-blocking 가정 (골 진행 중 채택)

- "명사형 종결" 판정 기준: 핵심 서술어가 명사(형 전성어미: ~함/~임/~것/~각/~중/~놈/~수준/~자리/~국룰 등)로 끝나면 통과, 뒤에 붙는 ㄹㅇ/ㅇㅈ/ㅋㅋ 등 추임새는 판정에서 제외(회차3). 사용자 예시 "이거 매수하면 주갤에서 박제당함"의 핵심이 서술어 구조이지 추임새 유무가 아니라는 근거.
