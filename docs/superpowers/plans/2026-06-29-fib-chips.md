# Fib 칩 — 차트 밖 HTML로 이동 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fibonacci 레벨 박스를 matplotlib 차트 이미지에서 제거하고 HTML 칩 한 줄로 차트 아래에 표시한다.

**Architecture:** 렌더러에서 Fib 그리기 코드를 제거 → app.py 에서 동일한 계산을 하여 API 응답에 `fib_levels` 배열 추가 → 프론트엔드 JS가 칩을 생성해 차트 아래 div에 삽입.

**Tech Stack:** Python(matplotlib 제거), Flask(API 페이로드), Vanilla JS(DOM), pytest

## Global Constraints

- Python 3.11+, 기존 테스트 모두 통과 유지
- JS — 외부 라이브러리 추가 없음, Vanilla JS만
- 스타일 — CSS custom property(`var(--text-secondary)` 등) 사용, 인라인 하드코드 색상은 칩 내부로 제한
- `fib_levels`가 `null`이면 칩 div 노출 금지

---

### Task 1: 렌더러에서 Fib 제거 + 테스트 업데이트

**Files:**
- Modify: `handdrawn_renderer.py:132-170` (\_\_init\_\_ 파라미터), `handdrawn_renderer.py:293-357` (Fib 렌더 블록)
- Modify: `tests/test_handdrawn_layers.py`

**Interfaces:**
- Produces: `HandDrawnChartRenderer.__init__`에서 `show_fib` 파라미터 없음

- [ ] **Step 1: 실패할 테스트 작성**

`tests/test_handdrawn_layers.py` 상단에 추가:

```python
def test_renderer_has_no_show_fib_param():
    """show_fib 파라미터가 제거됐는지 확인."""
    import inspect
    from handdrawn_renderer import HandDrawnChartRenderer
    sig = inspect.signature(HandDrawnChartRenderer.__init__)
    assert 'show_fib' not in sig.parameters, "show_fib 파라미터가 아직 남아있음"
```

- [ ] **Step 2: 테스트 실패 확인**

```
pytest tests/test_handdrawn_layers.py::test_renderer_has_no_show_fib_param -v
```
Expected: **FAIL** — "show_fib 파라미터가 아직 남아있음"

- [ ] **Step 3: handdrawn_renderer.py — \_\_init\_\_ 파라미터 제거**

`handdrawn_renderer.py` 에서:

```python
# 제거 전 (132~170 부근)
def __init__(self, hist: pd.DataFrame, result: FourAxisResult,
             ticker: str = "", lookback: int = 120,
             width_px: int = 720, height_px: int = 600, dpi: int = 100,
             support: float | None = None,
             resistance: float | None = None,
             show_fib: bool = True,       # ← 이 줄 삭제
             show_sr: bool = True,
             nomura_score_data: dict | None = None):
    ...
    self._show_fib   = show_fib          # ← 이 줄 삭제
    self._show_sr    = show_sr
```

변경 후:

```python
def __init__(self, hist: pd.DataFrame, result: FourAxisResult,
             ticker: str = "", lookback: int = 120,
             width_px: int = 720, height_px: int = 600, dpi: int = 100,
             support: float | None = None,
             resistance: float | None = None,
             show_sr: bool = True,
             nomura_score_data: dict | None = None):
    ...
    self._show_sr    = show_sr
```

- [ ] **Step 4: handdrawn_renderer.py — Fib 렌더 블록 제거**

`render()` 메서드 내부에서 아래 주석부터 `except Exception: pass` 까지 전체 삭제:

```python
            # ── ⑤ Fibonacci 텍스트 박스 (가격 패널 우측 중간, 다색) ──────
            if self._show_fib and len(self.hist) > 1:
                try:
                    ...  (약 65줄)
                except Exception:
                    pass
```

삭제 후 바로 다음 주석 `# ── ⑥ S/R 수평점선` 이 이어져야 한다.

- [ ] **Step 5: tests/test_handdrawn_layers.py — 기존 Fib 테스트 수정**

`show_fib=True` / `show_fib=False` 키워드 인수를 사용하는 모든 생성자 호출에서 해당 인수를 제거한다. 영향받는 함수:
- `test_renderer_fib_layers_no_crash`: `show_fib=True,` 줄 삭제
- `test_renderer_fib_disabled_no_crash`: `show_fib=False,` 줄 삭제
- `test_fib_labels_use_f_symbol`: `show_fib=True` 삭제; 함수 이름을 `test_no_fib_text_in_chart`로 변경하고, **렌더러가 Fib 텍스트를 생성하지 않는지** 검증하도록 수정:

```python
def test_no_fib_text_in_chart():
    """렌더러가 차트 이미지에 Fib 텍스트를 그리지 않아야 한다."""
    from unittest.mock import patch
    import matplotlib.pyplot as plt
    from handdrawn_renderer import HandDrawnChartRenderer

    hist = _make_hist()
    captured_texts = []

    original_text = plt.Axes.text
    def mock_text(self, x, y, s, *args, **kwargs):
        captured_texts.append(str(s))
        return original_text(self, x, y, s, *args, **kwargs)

    with patch.object(plt.Axes, "text", mock_text):
        r = HandDrawnChartRenderer(hist, _DummyResult(), ticker="TEST")
        r.render()

    fib_texts = [t for t in captured_texts
                 if any(pct in t for pct in ["23%", "38%", "50%", "62%", "79%"])]
    assert len(fib_texts) == 0, f"차트에 Fib 텍스트가 남아있음: {fib_texts}"
```

- `test_fib_labels_clip_on_false`: 렌더러에서 Fib 코드가 제거됐으므로 테스트 전체 삭제.

- [ ] **Step 6: 전체 테스트 통과 확인**

```
pytest tests/test_handdrawn_layers.py -v
```
Expected: 모든 테스트 **PASS**

- [ ] **Step 7: 커밋**

```bash
git add handdrawn_renderer.py tests/test_handdrawn_layers.py
git commit -m "refactor(renderer): Fib 렌더링 matplotlib에서 제거"
```

---

### Task 2: app.py — fib_levels 계산 및 payload 추가

**Files:**
- Modify: `web_app/app.py` (`_compute_four_axis_payload` 함수 내부)

**Interfaces:**
- Consumes: `hist` — pandas DataFrame, 컬럼 `High`, `Low` 존재
- Produces: API JSON에 `"fib_levels": list[{"pct": str, "price": float, "key": bool}] | null` 추가

- [ ] **Step 1: 실패할 테스트 작성**

`tests/test_handdrawn_layers.py` 마지막에 추가:

```python
def test_fib_levels_payload_shape():
    """fib_levels 계산 로직이 올바른 구조를 반환해야 한다."""
    import pandas as pd
    import numpy as np

    # 동일한 로직을 인라인으로 검증
    dates = pd.date_range("2024-01-01", periods=30)
    hist = pd.DataFrame({
        "High":  np.linspace(100, 120, 30),
        "Low":   np.linspace(80, 90, 30),
        "Close": np.linspace(90, 110, 30),
        "Open":  np.linspace(89, 109, 30),
        "Volume": np.ones(30) * 1000,
    }, index=dates)

    h_max = float(hist["High"].max())   # 120.0
    h_min = float(hist["Low"].min())    # 80.0
    lvls = [
        (0.236, "23%", False),
        (0.382, "38%", True),
        (0.5,   "50%", True),
        (0.618, "62%", True),
        (0.786, "79%", False),
    ]
    fib_levels = [
        {"pct": sym, "price": round(h_min + (h_max - h_min) * r, 2), "key": key}
        for r, sym, key in lvls
    ]

    assert len(fib_levels) == 5
    assert fib_levels[0] == {"pct": "23%", "price": round(80 + 40 * 0.236, 2), "key": False}
    assert fib_levels[1]["key"] is True
    assert fib_levels[2]["key"] is True
    assert fib_levels[3]["key"] is True
    assert fib_levels[4]["key"] is False
    for item in fib_levels:
        assert "pct" in item and "price" in item and "key" in item
```

- [ ] **Step 2: 테스트 통과 확인 (로직 검증용)**

```
pytest tests/test_handdrawn_layers.py::test_fib_levels_payload_shape -v
```
Expected: **PASS** (순수 계산 테스트이므로 즉시 통과)

- [ ] **Step 3: app.py — `show_fib=True` 제거 및 fib_levels 계산 삽입**

`web_app/app.py` 의 `_compute_four_axis_payload` 함수 내부, `renderer = HandDrawnChartRenderer(` 호출 직전에 삽입:

```python
# Fib 레벨 계산 — 렌더러에서 뺀 뒤 payload로 전달
_fib_levels = None
if hist is not None and len(hist) > 1:
    _h_max = float(hist["High"].max())
    _h_min = float(hist["Low"].min())
    _fib_lvls = [
        (0.236, "23%", False),
        (0.382, "38%", True),
        (0.5,   "50%", True),
        (0.618, "62%", True),
        (0.786, "79%", False),
    ]
    _fib_levels = [
        {"pct": sym, "price": round(_h_min + (_h_max - _h_min) * r, 2), "key": key}
        for r, sym, key in _fib_lvls
    ]
```

같은 함수의 `renderer = HandDrawnChartRenderer(` 호출에서 `show_fib=True,` 줄 삭제.

- [ ] **Step 4: payload dict에 fib_levels 추가**

`payload = { ... }` 블록에서 `"chart": chart_b64,` 바로 다음 줄에 추가:

```python
"fib_levels": _fib_levels,
```

- [ ] **Step 5: 서버 스모크 테스트**

Flask 개발 서버를 실행하고 실제 종목(예: AAPL)으로 API 응답 확인:

```bash
cd web_app && python app.py
# 별도 터미널에서:
curl "http://localhost:5000/api/four_axis/AAPL?market=US" | python -m json.tool | grep -A 20 fib_levels
```

Expected: `fib_levels` 배열 5개 항목, `chart` 이미지에 Fib 박스 없음.

- [ ] **Step 6: 커밋**

```bash
git add web_app/app.py tests/test_handdrawn_layers.py
git commit -m "feat(api): fib_levels 페이로드 추가, 렌더러 show_fib 제거"
```

---

### Task 3: 프론트엔드 — HTML 칩 div + JS 렌더링

**Files:**
- Modify: `web_app/templates/detail.html:1231-1235` (차트 wrap 아래에 div 추가)
- Modify: `web_app/static/app.js` (`loadFourAxis` 함수, 약 line 4897)

**Interfaces:**
- Consumes: `d.fib_levels` — `Array<{pct: string, price: number, key: boolean}>` | null (Task 2 produces)

- [ ] **Step 1: detail.html — #fib-chips div 삽입**

`web_app/templates/detail.html` 에서 아래 블록을 찾아:

```html
        <div id="fouraxis-chart-wrap" style="display:none;">
          <img id="fouraxis-chart" src="" alt="핸드드로윙 차트"
               style="width:100%; max-width:520px; display:block; margin:0 auto; border-radius:var(--radius); border:1px solid var(--border); opacity:.92;" />
        </div>
      </div>
```

`</div>\n      </div>` 사이에 다음을 삽입 (`</div>` 첫 번째는 `fouraxis-chart-wrap` 닫기):

```html
        <div id="fouraxis-chart-wrap" style="display:none;">
          <img id="fouraxis-chart" src="" alt="핸드드로윙 차트"
               style="width:100%; max-width:520px; display:block; margin:0 auto; border-radius:var(--radius); border:1px solid var(--border); opacity:.92;" />
        </div>
        <!-- Fibonacci 칩 -->
        <div id="fib-chips" style="display:none; padding:4px 0 2px; gap:6px; flex-wrap:wrap; align-items:center;"></div>
      </div>
```

- [ ] **Step 2: app.js — loadFourAxis 캐시 경로에 Fib 칩 추가**

`web_app/static/app.js` 에서 캐시 히트 시 차트를 보여주는 블록을 찾아:

```js
  if (cached) {
    document.getElementById('fouraxis-chart').src = 'data:image/png;base64,' + cached.chart;
    chartW.style.display = 'block';
    loading.style.display = 'none';
    return;
  }
```

아래처럼 교체:

```js
  if (cached) {
    document.getElementById('fouraxis-chart').src = 'data:image/png;base64,' + cached.chart;
    chartW.style.display = 'block';
    loading.style.display = 'none';
    _renderFibChips(cached.fib_levels);
    return;
  }
```

- [ ] **Step 3: app.js — loadFourAxis 신규 응답 경로에 Fib 칩 추가**

같은 함수에서 차트 src를 세팅하는 블록을 찾아:

```js
    _clientCache.set(cacheKey, d);
    document.getElementById('fouraxis-chart').src = 'data:image/png;base64,' + d.chart;
    chartW.style.display = 'block';
    _renderRsRating(d.rs_rating_data);
    _renderHeatSignal(d.heat_signal);
```

아래처럼 교체:

```js
    _clientCache.set(cacheKey, d);
    document.getElementById('fouraxis-chart').src = 'data:image/png;base64,' + d.chart;
    chartW.style.display = 'block';
    _renderFibChips(d.fib_levels);
    _renderRsRating(d.rs_rating_data);
    _renderHeatSignal(d.heat_signal);
```

- [ ] **Step 4: app.js — _renderFibChips 헬퍼 함수 추가**

`loadFourAxis` 함수 **앞** (같은 파일 내 인접 위치)에 아래 함수를 삽입:

```js
function _renderFibChips(fibLevels) {
  const wrap = document.getElementById('fib-chips');
  if (!wrap) return;
  if (!fibLevels || !fibLevels.length) {
    wrap.style.display = 'none';
    return;
  }
  wrap.innerHTML = '<span style="font-size:11px;font-weight:700;color:var(--text-secondary);white-space:nowrap;">Fib 120d</span>';
  fibLevels.forEach(f => {
    const chip = document.createElement('span');
    chip.textContent = `${f.pct} ${f.price.toLocaleString()}`;
    chip.style.cssText = f.key
      ? 'background:#ede9fe;color:#7c3aed;border-radius:20px;padding:3px 10px;font-size:11px;font-weight:600;white-space:nowrap;'
      : 'background:#f1f0ff;color:#aaa;border-radius:20px;padding:3px 10px;font-size:11px;white-space:nowrap;';
    wrap.appendChild(chip);
  });
  wrap.style.display = 'flex';
}
```

- [ ] **Step 5: 브라우저 확인**

Flask 개발 서버를 실행하고 종목 상세 페이지에서:
1. 차트 이미지 안에 Fib 박스 없음 확인
2. 차트 아래 "Fib 120d" + 5개 칩 표시 확인 (23%·79% 회색, 38%·50%·62% 보라)
3. 두 번째 방문(캐시) 시에도 칩이 표시되는지 확인

- [ ] **Step 6: 전체 테스트 통과 확인**

```
pytest tests/ -v
```
Expected: 모든 테스트 **PASS**

- [ ] **Step 7: 커밋**

```bash
git add web_app/templates/detail.html web_app/static/app.js
git commit -m "feat(ui): 차트 아래 Fib 칩 HTML 추가"
```
